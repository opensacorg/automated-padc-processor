# Core data processing libraries
pandas>=1.5.0,<3.0.0
openpyxl>=3.0.0,<4.0.0

# Progress bars and UI feedback
tqdm>=4.65.0,<5.0.0

# Dashboard formatting
prettytable>=3.0.0,<4.0.0

# Note: tkinter is typically included with Python installations
# If tkinter is not available, install it via your system package manager:
# - Ubuntu/Debian: sudo apt-get install python3-tk
# - macOS (Homebrew): brew install python-tk
# - Windows: Usually pre-installed with Python

# Optional: For better Excel performance
xlrd>=2.0.0,<3.0.0  # For reading older .xls files (optional)

# Development dependencies (optional)
# pytest>=7.0.0  # For testing
# black>=22.0.0  # For code formatting
# flake8>=5.0.0  # For linting

